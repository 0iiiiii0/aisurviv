# V260.4 绝密搜打撤四人组队进入链路修复

## 审查的完整进入链路

1. `client/src/siteInfo.ts`：把 `extraction_secret_squad` 映射到四人按钮。
2. `client/src/main.ts`：四人按钮写入 mode index，`tryJoinTeam(true)` 复验 `/api/account/profile`。
3. `client/src/ui/teamMenu.ts`：连接同源 `/team_v2`，create/join/playGame 均携带最新 account token。
4. `server/src/teamMenu.ts`：create、join、setRoomProps、playGame 对搜打撤成员做账号复验。
5. `server/src/apiServer.ts`：匹配前再次校验队长账号。
6. `server/src/devServer.ts` / `server/src/gameServer.ts`：本地区服匹配。
7. `server/src/game/gameManager.ts` / `gameProcessManager.ts`：创建/选择游戏房间。
8. `server/src/game/game.ts`：按 `playerCount=4` 生成四次可用的共享 match token。
9. `server/src/game/objects/player.ts`：四名玩家用同一 matchPriv 加入、按 loadoutPriv 区分账号，并逐人执行绝密装备资格检查。

## 本次确认并修复的问题

### 1. 已登录队伍在开始匹配时可能被第二次错误判定为未登录

旧链路：`TeamMenu -> ApiServer.findGame()` 已经验证 accountToken，随后删除 token，再通过 `Config.regions.local.address`（可能来自 `SURVIV_PUBLIC_ADDR`）HTTP 请求本机 `/api/find_game`。

该内部请求只有被识别为 loopback 时才能仅凭 apiKey 通过。如果公网 IP / 域名 / NAT / 反向代理令这次服务器自请求表现为非 loopback，则账号 token 又已经被删除，最终得到 `login_required`。

修复：集成式 `devServer` 注册 `localFindGameHandler`。本地区服在 API 层完成账号认证后直接调用本进程的 `GameServer.findGame()`，不再绕公网地址给自己发第二次 HTTP 请求；账号 token 仍不会传给游戏进程。

### 2. 四人中任意一个队友登录失效，会错误提示“队长未登录”

旧 `allAccountsValid(room)` 只返回 boolean。只要四人中任意一人会话过期，`playGame` 就把 `login_required` 发给队长，因此已登录队长也会看到“搜打撤模式需要登录账号”。

修复：改为收集具体失效成员。`login_required` 只发给真正 token/cookie 无效的成员，不再错误归因给有效队长。

### 3. 原回归测试实际上没有覆盖绝密四排

旧 `extractionTeamEnterSmokeTest.ts` 只测试 duo，而且 `addJoinToken(..., 2, ...)` 写死为两次使用。第 3/4 名玩家从未进入测试。

修复：共享 token 使用次数改为 `accounts.length`，新增普通四排与绝密四排 4 人实测；新增 TeamMenu 四账号 create -> 3 join -> playGame 回归，并专门测试“第 4 人会话失效不能误报队长”。

## 回归结果

- Server TypeScript `--noEmit`：通过
- Client TypeScript `--noEmit`：通过
- TeamMenu smoke：通过
- Guest/extraction auth smoke：通过
- Internal find-game authorization smoke：通过
- 普通搜打撤 duo：2 人共享 token 进入：通过
- 普通搜打撤 squad：4 人共享 token 进入：通过
- 绝密搜打撤 duo：2 人合格配装进入：通过
- 绝密搜打撤 squad：4 人合格配装进入同组：通过
- 实际 `devServer.js:8001`，4 个真实账号，绝密 squad mode 44：通过
- 端到端测试故意设置不可达 `SURVIV_PUBLIC_ADDR=203.0.113.123:8001`：仍成功返回四人 `joinGame`，证明本地匹配不再依赖公网自请求。
