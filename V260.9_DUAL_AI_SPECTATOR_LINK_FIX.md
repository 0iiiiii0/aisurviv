# V260.9 双 AI 观战链接直达修复

## 现象
后台“双方 AI”创建成功并生成 `/?duelWatch=XXXXXXXX` 链接，但打开链接只停留在主界面。

## 根因
后台正确生成了 `duelWatch` 分享码参数，服务端也已经实现 `/api/duel-lobby` 的 `watch` 动作；但客户端从未读取 `duelWatch`。客户端启动时只处理普通 `duelLobby` 邀请、`adminSpectate` 和持久化 `gameId + token`，因此双 AI 分享链接被静默忽略。

## 修复
- `DuelLobby.init()` 优先识别 `duelWatch`。
- 8 位分享码通过 `/api/duel-lobby` + `action: "watch"` 兑换当前浏览器独立的 spectator token。
- 成功后以 `spectator=true` 调用现有进局回调，直接进入双 AI 对局。
- 同一个分享码可以被多个观战者重复使用；每个观战者获得自己的 join token。
- 分享码兑换成功后从 URL 删除 `duelWatch`；随后 `joinGame()` 写入真正的 `gameId + token`，保留刷新/重连能力。
- 无效或已过期分享码显示明确错误，不再静默留在主界面。
- 生产 bundle 使用新的文件名，避免浏览器继续命中旧 bundle 缓存。

## 验证
- Client TypeScript `--noEmit`：通过。
- 浏览器启动行为模拟：`/?duelWatch=ABCDEFGH` 会 POST `{action:"watch", shareCode:"ABCDEFGH"}`，并以 `spectator=true` 进入。
- 双 AI 创建 smoke：通过。
- spectator share 多人分享 smoke：通过。
- Duel lobby / duel gameplay：通过。
- spectator auto-fill / difficulty / supervisor：通过。
- 生产 bundle `node --check`：通过。
- `NODE_ENV=production` 实际启动 `:8001`：通过；首页实际加载新 bundle。
