# V260.8 僵尸简单难度 / 房间名称修复

## 1. 简单难度完全禁用自爆僵尸

- `ZOMBIE_DIFFICULTY_PRESETS.simple.selfDestructChance` 从 3% 改为 0%。
- 在 `ZombieModeSystem.spawnZombie()` 增加最终安全闸：当房间难度为 `simple` 时，无论调用方传入多少自爆概率，最终概率都强制为 0。
- 因此开局、补充刷新以及未来误传的波次参数都不能在简单难度生成自爆僵尸。

## 2. 僵尸活跃房间名称增加难度前缀

统一格式：

- `【简单】僵尸模式 单人/双人/四人`
- `【普通】僵尸模式 单人/双人/四人`
- `【困难】僵尸模式 单人/双人/四人`

覆盖：

- 玩家观战房间列表 `/api/spectate/rooms`
- 后台实时房间列表

非法或缺失的难度值按 `普通` 显示，与匹配系统原有回退规则一致。

## 3. 回归测试

通过：

- Server TypeScript `--noEmit`
- Client TypeScript `--noEmit`
- 简单难度 30 只初始僵尸全部非自爆
- 简单难度即使强制向生成函数传入 100% 自爆概率，仍不能生成自爆僵尸
- 僵尸 Duo/Squad 与真人/僵尸队伍隔离
- 自爆僵尸 LOS
- 自爆僵尸 committed rush 连续性
- 僵尸可见性
- 僵尸无毒气
- 僵尸任务 / 核爆成就
- 生产多进程 simple/normal/hard 房间隔离
- 三种难度房间名前缀
- 后台完整 Admin smoke test
- All-modes smoke test
- 后台 public/dist JS 语法检查

同时修正了一个已过期的后台测试：旧测试把 1v1 播放列表索引写死为 3；新增僵尸播放列表后该假设已失效。现在测试按 `mapName === "duel"` 动态查找索引，不影响游戏逻辑。

## 4. 测试稳定性修正

`zombieSelfDestructRushContinuitySmokeTest` 原先最后一步固定把自爆僵尸放到玩家 `x+2`，在随机地图种子下该点偶尔可能落在障碍边缘，造成假失败。现在沿前面已经验证为可通行的冲锋通道移动到 2 单位贴脸位置；连续 5 次不同随机地图运行均通过。
