# V49 验证报告

## 构建和语法

- 服务端 TypeScript：通过
- 客户端 TypeScript `--noEmit`：通过
- 后台 `admin.js`：Node 语法检查通过
- 后台 `adminInputHelpers.js`：Node 语法检查通过
- `git diff --check`：通过
- 从 V48 基线 `63fcc86` 执行 `git apply --check` 和实际应用：通过
- 补丁应用后的服务端构建及 V49 专项测试：通过

## 自动测试

- 服务端测试脚本：68/68 通过
- V49 专项测试：通过
- 资源碰撞审计：170 种定义、3360 个变换场景，全部通过
- 50v50 自动补入：通过
- 后台 API：通过
- 多模式模拟：通过
- 弹药分享模拟：通过
- 资源搜刮和开局资源扫描：通过

完整日志：`V49_FULL_TEST.log`

## 录像驱动验证

- 旧录像无掉落资源目标：593/770（77.01%）
- 修复后这些目标的 `searchLootEligible` 均为 false
- `crate_01` 等真实掉落资源仍为 true
- 玩家 4127：旧录像 40 次请求观察、0 次投送
- 新回归验证：重复请求保留首次时间；真人请求优先；半弹匣保留策略可完成投送

## 仍需实机观察

自动测试无法完全替代真实浏览器和多人 50v50 长局。建议实机重点确认：

- 小地图同阵营标记在多人陆续加入、死亡和观战切换后持续正确
- 真人连续发送不同口径请求时，最近的有弹药 AI 能到达并投送
- AI 不再把普通树、阵营石头和窗户作为搜刮目标，但被路径堵住时仍能破障
