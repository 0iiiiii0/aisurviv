# V210 验证记录：多进程一致性

## 验证

1. 锁互斥（跨实例交替写不覆盖）✓
2. 读前重载（写入立即可见）✓
3. 唯一 tmp + rename 原子写 ✓
4. 失败保留、下次重试 ✓
5. stale 锁自动清理（15s）✓
6. 构建与回归：server tsc、test:extraction /
   admin / all-modes PASS ✓

## 结论

- 多进程下仓库数据一致，不丢失、不覆盖；
- API 配装与房间撤离结果跨进程即时可见。
