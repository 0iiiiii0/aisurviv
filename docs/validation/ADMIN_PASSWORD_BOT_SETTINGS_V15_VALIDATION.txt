V15 validation summary
======================

Passed:
- Server TypeScript build: `server/npm run build`
- Client TypeScript check: `client/npx tsc --noEmit`
- Admin JavaScript syntax check: `node --check client/public/admin/admin.js`
- Admin public/static dist files are byte-identical after synchronization
- Admin service smoke test, including bot auto-fill persistence callbacks
- Admin authentication smoke test: salted scrypt storage, login, logout, password rotation, session invalidation
- Bot auto-fill config smoke test: live per-mode cadence, ordinary caps/targets, faction total cap distribution
- 50v50 auto-fill smoke test
- Spectator/auto-fill regression smoke test
- 47-playlist / 17-map all-mode smoke test
- Live HTTP integration test:
  - correct password login returned HTTP 200
  - unauthenticated status returned HTTP 401
  - incorrect password returned HTTP 401
  - bot settings update returned HTTP 200 and persisted to survivio-config.json
  - password change returned HTTP 200
  - old session and old password returned HTTP 401
  - new password login returned HTTP 200
  - credential file contained no plaintext password

Packaging note:
- The admin assets are plain files under client/public/admin and were copied to client/dist/admin.
- A full Vite build was not required for these static admin assets. The Linux validation container lacks Rollup's optional native package, but client TypeScript validation passed.
